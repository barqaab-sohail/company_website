@extends('layouts.site') @section('title',ucfirst($type).' | BARQAAB') @section('content')<section class="page-head"><p class="eyebrow">BARQAAB</p><h1>{{ $type === 'post' ? 'News & Insights' : ucfirst($type).'s' }}</h1></section><section class="work"><div class="grid">@forelse($items as $item)<a class="card" href="{{ route('content.show',$item->slug) }}">@if($item->featured_image)<img src="{{ $item->featured_image }}" alt="{{ $item->title }}" loading="lazy" decoding="async">@endif<div><small>{{ ucfirst($item->type) }}</small><h3>{{ $item->title }}</h3><p>{{ $item->excerpt }}</p></div></a>@empty<p>No published content yet.</p>@endforelse</div>{{ $items->links() }}</section>@endsection
