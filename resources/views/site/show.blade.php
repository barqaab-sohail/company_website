@extends('layouts.site') @section('title',$content->title.' | BARQAAB') @section('content')<section class="page-head"><p class="eyebrow">{{ ucfirst($content->type) }}</p><h1>{{ $content->title }}</h1></section><article class="content">@if($content->featured_image)<img class="feature" src="{{ $content->featured_image }}" alt="{{ $content->title }}">@endif<div class="prose">{!! $content->body !!}</div>@if($content->type==='job')<form method="post" enctype="multipart/form-data" action="{{ route('jobs.apply',$content) }}">@csrf<h2>Apply for this position</h2><input name="name" placeholder="Full name" required><input type="email" name="email" placeholder="Email" required><input name="phone" placeholder="Phone"><textarea name="cover_letter" placeholder="Cover letter"></textarea><input type="file" name="resume"><button class="button">Submit application</button></form>@endif</article>@endsection
